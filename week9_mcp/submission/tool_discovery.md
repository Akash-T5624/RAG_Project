# Live `tools/list` discovery

**1 before -> 3 after**

Before: policy_documents.search_policy_documents

After: policy_documents.search_policy_documents, claims_system.get_claim_status, claims_system.get_adjuster_note_history

These names were collected by live `tools/list` responses, not copied from server source.
